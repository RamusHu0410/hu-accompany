[package]
name = "native-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"] # Required for Flutter FFI compilation

[dependencies]
cpal = "0.15"
core-logic = { path = "../core-logic" }
once_cell = "1.21.4"
