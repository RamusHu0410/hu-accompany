"""Phase 4 - Composer Marking Analysis (STUB).

Spec: OCR detected text regions, recognize tempo/dynamics/expressions/
fingerings/etc., and reconnect markings (slurs, hairpins, pedal, octave
shifts) that span multiple systems.

Not implemented yet - passes classified objects through unchanged.
"""

import logging


def analyze_markings(objects: list, config, logger: logging.Logger) -> list:
    logger.info("phase4_markings: not yet implemented, passing objects through unchanged")
    return objects
