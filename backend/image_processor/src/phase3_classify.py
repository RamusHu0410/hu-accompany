"""Phase 3 - Object Classification (STUB).

Spec: assign each detected object a primary classification plus alternative
candidate labels with confidences (never a single forced interpretation when
uncertainty exists).

Not implemented yet - passes detected objects through unchanged.
"""

import logging


def classify(objects: list, config, logger: logging.Logger) -> list:
    logger.info("phase3_classify: not yet implemented, passing objects through unchanged")
    return objects
