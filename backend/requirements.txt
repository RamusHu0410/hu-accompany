# Web framework
django

# Config / environment
python-dotenv

# HTTP client / LAN service discovery (api/mdns.py advertises the
# Django server so the Flutter app can find it without a fixed IP)
requests
zeroconf

# IMSLP score download (imslp_downloader app drives a real browser
# through IMSLP's disclaimer/subscribe flow)
playwright

# PDF -> image extraction (image_processor phase1_prepare, scripts/pdf_processor.py)
PyMuPDF
# image_enhancer/enhancer_script.py's scan cleanup. Needs the libvips system
# library too (not pip-installable): `brew install vips` on macOS,
# `apt install libvips` on Debian/Ubuntu.
pyvips
# Pinned: opencv 5.0 changed cv2.HoughLinesP's return shape, which breaks
# oemer's line parsing (oemer/bbox.py) with an IndexError.
opencv-python-headless==4.11.0.86
numpy

# Optical music recognition pipeline (image_processor phase2_detect, scripts/pdf_processor.py)
oemer
music21

# OCR (pdf_processor/part2_markings -- composer marking detection; pdfplumber
# is legacy archive/Converter script only)
pdfplumber
pytesseract

# LLM client -- currently unused (imslp_search/main.py has a Gemini
# integration commented out); keep or drop depending on whether that
# code path is coming back
google-genai
alembic==1.19.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
boto3==1.43.83
botocore==1.43.83
cffi==2.1.1
click==8.5.0
fastapi==0.141.1
h11==0.16.0
idna==3.19
img2pdf==0.6.3
jmespath==1.1.0
lxml==6.1.2
mako==1.4.1
markupsafe==3.0.3
numpy==2.5.2
packaging==26.3
pandas==3.0.5
pikepdf==10.12.0
pillow==12.3.0
pip==26.2.1
pycparser==3.0
pydantic==2.13.5
pydantic-core==2.46.5
python-dateutil==2.9.0.post0
pyvips==3.1.1
s3transfer==0.19.2
six==1.17.0
sqlalchemy==2.0.52
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
uvicorn==0.52.4
